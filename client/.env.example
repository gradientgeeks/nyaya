# Nyaya Client Environment Variables

# Backend API URL (used when running frontend separately from backend)
# Leave empty to use relative URLs (proxied in dev mode)
VITE_API_URL=http://localhost:8000
